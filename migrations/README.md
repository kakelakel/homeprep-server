# Database migrations

Alembic migration scripts for HomePrep Server live here.

The first schema migration will be added together with the Household and Inventory persistence model. Migrations must be forward-only in normal operation and must be exercised in automated tests before release.
